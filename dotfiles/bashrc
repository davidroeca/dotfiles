include () {
  [[ -f "$1" ]] && source "$1"
}

LOCALPATH=~/.bashrc_local
MINTDEFAULT=~/.bashrc_mint
include $LOCALPATH
include $MINTDEFAULT

alias vn='virtualenv -p python3 venv'
alias va='source venv/bin/activate'

[[ $TERM != screen* ]] && exec tmux -2
