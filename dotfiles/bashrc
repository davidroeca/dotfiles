include () {
  [[ -f "$1" ]] && source "$1"
}

LOCALPATH=~/.bashrc_local
MINTDEFAULT=~/.bashrc_mint
include $LOCALPATH
include $MINTDEFAULT

alias vn='python3 -m venv venv'
alias va='source venv/bin/activate'

alias tmux='tmux -2'
alias tn='tmux new-session'
alias tl='tmux ls'
alias ta='tmux attach -t'
alias tk='tmux kill-session -t'
